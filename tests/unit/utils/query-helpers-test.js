import queryHelpers from 'dummy/utils/query-helpers';
import { module, test } from 'qunit';

module('Unit | Utility | query helpers');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = queryHelpers();
  assert.ok(result);
});
